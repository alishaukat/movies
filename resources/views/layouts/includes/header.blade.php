<!-- Page Header -->
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
            @if(!empty($heading))
            {{ $heading }}
            @endif
            @if(!empty($subHeading))
            <small>{{ $subHeading }}</small>
            @endif
        </h1>
    </div>
</div>